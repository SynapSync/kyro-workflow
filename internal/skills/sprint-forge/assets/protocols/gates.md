# Gate Protocol

At lifecycle gates, report: completed work, evidence, risks, next action, and whether user approval is required.

Do not gate after every task. Gate after INIT, sprint planning, implementation completion, sprint close, and scope completion when the user asks to complete the scope.

Scope completion uses `{{KYRO_CLI}} scope complete` (preview, then `--yes`). It is not retirement.
