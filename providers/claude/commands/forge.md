---
description: "Route Kyro sprint work: plan, execute, review, close a sprint, or complete a finished scope. Not for obsolete or superseded scopes."
argument-hint: <scope or request>
---

# Kyro Forge

Read `${CLAUDE_PLUGIN_ROOT}/commands/forge.md` and follow it exactly.

When that router references `skills/...`, resolve the path under
`${CLAUDE_PLUGIN_ROOT}/internal/skills/...`. These internal assets are implementation
details and must not be presented as separate user commands.
